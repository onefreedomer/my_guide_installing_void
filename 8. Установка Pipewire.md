Установка Pipewire:
```bash
sudo xbps-install pipewire wireplumber alsa-pipewire libspa-bluetooth
```

Настройка менеджера сессий для Pipewire:
```bash
mkdir -p /etc/pipewire/pipewire.conf.d
ln -s /usr/share/examples/wireplumber/10-wireplumber.conf /etc/pipewire/pipewire.conf.d/
```

Настройка по перенаправлению звука PulseAudio к Pipewire:
```bash
ln -s /usr/share/examples/pipewire/20-pipewire-pulse.conf /etc/pipewire/pipewire.conf.d/
```

Включение Pipewire ALSA:
```bash
mkdir -p /etc/alsa/conf.d
ln -s /usr/share/alsa/alsa.conf.d/50-pipewire.conf /etc/alsa/conf.d
ln -s /usr/share/alsa/alsa.conf.d/99-pipewire-default.conf /etc/alsa/conf.d
```

Для совместной работы Pipewire с графическим окружением (X11, Wayland), он включается через dbus:
```bash
dbus-run-session pipewire &
```

